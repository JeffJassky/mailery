/**
 * Zod schemas for runtime validation at write boundaries (collection inserts,
 * REST payloads). Stub for Phase 0 — fill in alongside src/server/models.
 */

export const SCHEMAS_PLACEHOLDER = true
