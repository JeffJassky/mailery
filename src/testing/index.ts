// Test helpers — imported by host apps as `mailery/testing`.
// Phase 0 will export:
//   - createTestMailer({ ... }) → { mailer, db, redis, provider, adapter }
//   - NullProvider
//   - MemoryContactAdapter

export const TESTING_PLACEHOLDER = true
